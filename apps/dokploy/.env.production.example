PORT=3000
NODE_ENV=production

# CI / Docker build: без этого Next при импорте billing/payment раньше падал на .replace (см. TinkoffPayment).
# Совпадает с дефолтом в packages/server/src/billing/payment.ts.
TINKOFF_API_BASE_URL=https://securepay.tinkoff.ru/v2